OPTION(ENABLE_STMICRO_EXAMPLE "Enable example for STMicro" ON)
OPTION(ENABLE_NORDIC_EXAMPLE "Enable example for Nordic" ON)

IF(ENABLE_STMICRO_EXAMPLE)
    ADD_SUBDIRECTORY(STMicro)
ENDIF()

IF(ENABLE_NORDIC_EXAMPLE)
    ADD_SUBDIRECTORY(Nordic)
ENDIF()
