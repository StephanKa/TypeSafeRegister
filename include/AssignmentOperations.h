#pragma once

/** @brief Select bitwise OR assignment for Register::write(). */
struct OrAssign
{
};

/** @brief Select bitwise AND assignment for Register::write(). */
struct AndAssign
{
};

/** @brief Select bitwise XOR assignment for Register::write(). */
struct ExclusiveOrAssign
{
};
